import React from 'react';
import styles from './processHero.module.css';

const ProcessHero: React.FC = () => {
    return (
        <section id="hero" className={styles.compHeroContainer}>
            <h2>Expose the process:</h2>
            <p>
                please put placeholder for now
            </p>
            <p>
                (note to self: see HBR and Buy then Build for process details)
            </p>
        </section>
    );
};

export default ProcessHero;
